# Minigames

## Chinese Fortune Slot

The current minigame is a simple three-symbol slot presentation. `MainGameScene` owns the `BackgroundBlock` and `SlotBoardBlock`; the board positions the dragon, red lantern, and golden ingot using the board's source-pixel coordinates. No ice, snow, aurora, crystal, or mountain theme remains in the active game configuration.
